<div class="box">
  <div class="row">
  <%#div class="col-md-6 col-md-offset-3"%>
  <h1>
    <%#= gravatar_for @talkroom.current_user.name %>
    <%= @talkroom.talkname %>
  </h1>
  <% if logged_in? %>
      <div class="comments_line">
        <%#= render partial: 'comments/comment', collection: @comments %>
      </div>
      <div class="comment_form">
          <%= render 'comments/comment_form' %>
      </div>
  <% end %>
  </div>
  </div>
</div>