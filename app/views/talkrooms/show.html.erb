<div class="box">
  <div class="row">
    <div class="main-contents col-xs-8 col-xs-offset-2">
      <h1>
        <%= @talkroom.talkname %>
      </h1>
      <% if logged_in? %>
          <div id="comments-line">
            <%= render partial:'comments/feed' %>
          </div>
          <div class="comment_form">
            <%= render 'comments/comment_form', talkroom: @talkroom %>
          </div>
      <% end %>
    </div>
  </div>
</div>