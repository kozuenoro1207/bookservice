<%= form_for(current_user.favtalk_relationships.find_by(talkroom_id: talkroom.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= button_tag '<i class="onfav fa fa-heart fa-2x"></i>'.html_safe, :type => "submit", 
    :class =>"btn btn-link" %>
<% end %>