<%= form_for(current_user.favcom_relationships.build, remote: true) do |f| %>
    <span>
    <%= f.hidden_field :comment_id, :value => comment.id %>
    </span>
    <%= button_tag '<i class="notfav fa fa-heart fa-2x"></i>'.html_safe, :type => "submit", 
    :class =>"btn btn-link" %>
<% end %>
